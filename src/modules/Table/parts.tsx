import styled from 'styled-components';

export const TableWrapper = styled.div`
   position: relative;
   border: solid 1px green;
   display: inline-flex;
   overflow-x: scroll;
   width: 100%;
   `;
